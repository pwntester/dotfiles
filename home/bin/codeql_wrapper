#!/bin/bash
export LOGFILE=/tmp/codeql
echo `env` > $LOGFILE-env
echo "$@" >> $LOGFILE-arguments
if [ "$2" == "query-server" ]; then
    cat - | tee $LOGFILE-stdin | ~/codeql-home/codeql-cli/codeql $@ | tee $LOGFILE-stdout
else
    ~/codeql-home/codeql-cli/codeql $@
fi
