#!/bin/bash
export LOGFILE=/tmp/codeql
echo `env` > $LOGFILE-env
echo "$@" >> $LOGFILE-arguments

if [ "$2" == "language-server" ]; then
  cat - | tee -a $LOGFILE-stdin | ~/codeql-home/cli/codeql $@ 2>&1 | tee -a $LOGFILE-stdouterr
else
  ~/codeql-home/cli/codeql $@
fi
